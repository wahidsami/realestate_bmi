# 0001_initial

This folder is the first migration checkpoint for the Prisma schema.

Because the workspace currently does not have installed dependencies, Prisma CLI could not be executed here.
After installing dependencies, regenerate the SQL migration from `schema.prisma` and replace this scaffold if needed.
