# API Documentation Structure

This folder holds the backend API documentation scaffold.

Planned documents:

- `openapi.yaml` for API contract definition
- `authentication.md` for JWT usage
- `uploads.md` for file upload conventions
- `validation.md` for request schema rules
- `repositories.md` for backend data access patterns
